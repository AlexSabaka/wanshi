import { ComingSoon } from "@/components/coming-soon"

export default function ReviewPage() {
  return (
    <ComingSoon
      title="Review & curation"
      description="Human-in-the-loop: accept / reject / edit facts, merge / split entities, side-by-side with the source."
      note="Curation mode is the next build — the explorer + inspector spine and provenance it stands on are in place."
    />
  )
}
